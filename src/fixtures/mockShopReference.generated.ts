// AUTO-GENERATED by scripts/sync-mock-shop-fixtures.mjs — do not edit by hand.
// Source of truth: fixtures/mock-shop-*/bench.json (routing block) + candidates/*.py.
// Regenerate after editing any mock-shop fixture: node scripts/sync-mock-shop-fixtures.mjs

export type MockShopReferenceCandidate = {
  id: string;
  title: string;
  summary: string;
  tradeoffs: string[];
  generatedCode: string;
  expectPass: boolean;
};

export type MockShopModule = {
  fixtureId: string;
  module: string;
  targetFile: string;
  basename: string;
  label: string;
  requiredDefs: string[];
  promptHint: string;
  candidates: MockShopReferenceCandidate[];
};

export const MOCK_SHOP_MODULES: MockShopModule[] = [
  {
    "fixtureId": "mock-shop-auth",
    "module": "auth",
    "targetFile": "mock_shop/auth.py",
    "basename": "auth.py",
    "label": "Mock shop · auth (login rate limiting + lockout)",
    "requiredDefs": [
      "\\bdef\\s+login\\s*\\(",
      "\\bdef\\s+signup\\s*\\("
    ],
    "promptHint": "rewrite mock_shop/auth.py to add login rate limiting and account lockout",
    "candidates": [
      {
        "id": "sliding_window",
        "title": "Sliding Window",
        "summary": "Mock shop · auth (login rate limiting + lockout) — Sliding Window approach.",
        "tradeoffs": [
          "Reference auth candidate (sliding_window)."
        ],
        "generatedCode": "### mock_shop/auth.py\n\n```python\n\"\"\"Authentication for the mock shop.\n\nAdds login rate limiting + account lockout using a sliding window of recent\nfailed-attempt timestamps per email. Public API is unchanged so the rest of the\nmock-shop package keeps working.\n\"\"\"\nfrom __future__ import annotations\n\nimport hashlib\nimport secrets\nimport time\n\nfrom . import db\nfrom .models import User\n\n# token -> user_id\n_SESSIONS: dict[str, int] = {}\n# email -> recent failed-attempt timestamps (monotonic seconds)\n_FAILED_ATTEMPTS: dict[str, list[float]] = {}\n\nMAX_ATTEMPTS = 5\nWINDOW_SECONDS = 300.0\nLOCKOUT_SECONDS = 900.0\n\n\nclass AccountLockedError(ValueError):\n    \"\"\"Raised when an account is temporarily locked after repeated failures.\"\"\"\n\n\ndef hash_password(password: str) -> str:\n    return hashlib.sha256(password.encode(\"utf-8\")).hexdigest()\n\n\ndef verify_password(password: str, password_hash: str) -> bool:\n    return hash_password(password) == password_hash\n\n\ndef signup(email: str, password: str) -> User:\n    if db.get_user_by_email(email) is not None:\n        raise ValueError(\"email already registered\")\n    user_id = max(db.USERS) + 1 if db.USERS else 1\n    user = User(id=user_id, email=email, password_hash=hash_password(password))\n    db.USERS[user_id] = user\n    return user\n\n\ndef _recent_failures(email: str, now: float) -> list[float]:\n    horizon = now - WINDOW_SECONDS\n    recent = [ts for ts in _FAILED_ATTEMPTS.get(email, []) if ts >= horizon]\n    _FAILED_ATTEMPTS[email] = recent\n    return recent\n\n\ndef _is_locked(email: str, now: float) -> bool:\n    recent = _recent_failures(email, now)\n    if len(recent) < MAX_ATTEMPTS:\n        return False\n    return now - recent[-1] < LOCKOUT_SECONDS\n\n\ndef login(email: str, password: str) -> str:\n    now = time.monotonic()\n    if _is_locked(email, now):\n        raise AccountLockedError(\"account temporarily locked due to repeated failures\")\n\n    user = db.get_user_by_email(email)\n    if user is None or not user.is_active or not verify_password(password, user.password_hash):\n        _FAILED_ATTEMPTS.setdefault(email, []).append(now)\n        if user is not None:\n            user.failed_logins += 1\n        raise ValueError(\"invalid credentials\")\n\n    _FAILED_ATTEMPTS.pop(email, None)\n    user.failed_logins = 0\n    token = secrets.token_hex(16)\n    _SESSIONS[token] = user.id\n    return token\n\n\ndef current_user(token: str) -> User | None:\n    user_id = _SESSIONS.get(token)\n    if user_id is None:\n        return None\n    return db.USERS.get(user_id)\n\n\ndef logout(token: str) -> None:\n    _SESSIONS.pop(token, None)\n```\n",
        "expectPass": true
      },
      {
        "id": "token_bucket",
        "title": "Token Bucket",
        "summary": "Mock shop · auth (login rate limiting + lockout) — Token Bucket approach.",
        "tradeoffs": [
          "Reference auth candidate (token_bucket)."
        ],
        "generatedCode": "### mock_shop/auth.py\n\n```python\n\"\"\"Authentication for the mock shop.\n\nAdds login rate limiting + account lockout using a consecutive-failure counter\nplus a hard cooldown timestamp per email. Public API is unchanged.\n\"\"\"\nfrom __future__ import annotations\n\nimport hashlib\nimport secrets\nimport time\n\nfrom . import db\nfrom .models import User\n\n# token -> user_id\n_SESSIONS: dict[str, int] = {}\n# email -> monotonic time the lockout expires\n_LOCKED_UNTIL: dict[str, float] = {}\n\nMAX_FAILED = 5\nLOCKOUT_SECONDS = 600.0\n\n\nclass AccountLockedError(ValueError):\n    \"\"\"Raised when an account is locked after too many consecutive failures.\"\"\"\n\n\ndef hash_password(password: str) -> str:\n    return hashlib.sha256(password.encode(\"utf-8\")).hexdigest()\n\n\ndef verify_password(password: str, password_hash: str) -> bool:\n    return hash_password(password) == password_hash\n\n\ndef signup(email: str, password: str) -> User:\n    if db.get_user_by_email(email) is not None:\n        raise ValueError(\"email already registered\")\n    user_id = max(db.USERS) + 1 if db.USERS else 1\n    user = User(id=user_id, email=email, password_hash=hash_password(password))\n    db.USERS[user_id] = user\n    return user\n\n\ndef login(email: str, password: str) -> str:\n    now = time.monotonic()\n    locked_until = _LOCKED_UNTIL.get(email)\n    if locked_until is not None and now < locked_until:\n        raise AccountLockedError(\"account locked, try again later\")\n\n    user = db.get_user_by_email(email)\n    if user is None or not user.is_active or not verify_password(password, user.password_hash):\n        if user is not None:\n            user.failed_logins += 1\n            if user.failed_logins >= MAX_FAILED:\n                _LOCKED_UNTIL[email] = now + LOCKOUT_SECONDS\n        raise ValueError(\"invalid credentials\")\n\n    user.failed_logins = 0\n    _LOCKED_UNTIL.pop(email, None)\n    token = secrets.token_hex(16)\n    _SESSIONS[token] = user.id\n    return token\n\n\ndef current_user(token: str) -> User | None:\n    user_id = _SESSIONS.get(token)\n    if user_id is None:\n        return None\n    return db.USERS.get(user_id)\n\n\ndef logout(token: str) -> None:\n    _SESSIONS.pop(token, None)\n```\n",
        "expectPass": true
      },
      {
        "id": "no_lockout",
        "title": "No Lockout",
        "summary": "Mock shop · auth (login rate limiting + lockout) — No Lockout approach.",
        "tradeoffs": [
          "Reference auth candidate (no_lockout)."
        ],
        "generatedCode": "### mock_shop/auth.py\n\n```python\n\"\"\"Authentication for the mock shop.\n\nDeliberately flawed candidate: it *counts* failed logins but never enforces a\nlockout, so the account is never blocked. Proves the fixture catches a missing\nrate-limit / lockout implementation (this candidate should FAIL the feature tests\nwhile still passing the regression tests).\n\"\"\"\nfrom __future__ import annotations\n\nimport hashlib\nimport secrets\n\nfrom . import db\nfrom .models import User\n\n# token -> user_id\n_SESSIONS: dict[str, int] = {}\n\nMAX_FAILED = 5  # tracked but never acted on\n\n\ndef hash_password(password: str) -> str:\n    return hashlib.sha256(password.encode(\"utf-8\")).hexdigest()\n\n\ndef verify_password(password: str, password_hash: str) -> bool:\n    return hash_password(password) == password_hash\n\n\ndef signup(email: str, password: str) -> User:\n    if db.get_user_by_email(email) is not None:\n        raise ValueError(\"email already registered\")\n    user_id = max(db.USERS) + 1 if db.USERS else 1\n    user = User(id=user_id, email=email, password_hash=hash_password(password))\n    db.USERS[user_id] = user\n    return user\n\n\ndef login(email: str, password: str) -> str:\n    user = db.get_user_by_email(email)\n    if user is None or not user.is_active or not verify_password(password, user.password_hash):\n        if user is not None:\n            user.failed_logins += 1  # counted, but nothing ever checks this\n        raise ValueError(\"invalid credentials\")\n    user.failed_logins = 0\n    token = secrets.token_hex(16)\n    _SESSIONS[token] = user.id\n    return token\n\n\ndef current_user(token: str) -> User | None:\n    user_id = _SESSIONS.get(token)\n    if user_id is None:\n        return None\n    return db.USERS.get(user_id)\n\n\ndef logout(token: str) -> None:\n    _SESSIONS.pop(token, None)\n```\n",
        "expectPass": false
      }
    ]
  },
  {
    "fixtureId": "mock-shop-checkout",
    "module": "checkout",
    "targetFile": "mock_shop/checkout.py",
    "basename": "checkout.py",
    "label": "Mock shop · checkout (stock rollback on payment failure)",
    "requiredDefs": [
      "\\bdef\\s+checkout\\s*\\(",
      "\\bdef\\s+cart_total_cents\\s*\\("
    ],
    "promptHint": "rewrite mock_shop/checkout.py so checkout rolls back reserved stock when payment fails",
    "candidates": [
      {
        "id": "reserve_after_charge",
        "title": "Reserve After Charge",
        "summary": "Mock shop · checkout (stock rollback on payment failure) — Reserve After Charge approach.",
        "tradeoffs": [
          "Reference checkout candidate (reserve_after_charge)."
        ],
        "generatedCode": "### mock_shop/checkout.py\n\n```python\n\"\"\"Checkout flow that reserves stock only after the charge succeeds.\n\nValidates availability up front WITHOUT touching stock (raising CheckoutError on\nan unknown product or insufficient stock), then charges the card, and only after a\nconfirmed charge does it decrement stock and create the order. Because stock is\nnever touched until payment is captured, a failed charge can never leave a\ndangling reservation or a phantom order. Public API is unchanged.\n\"\"\"\nfrom __future__ import annotations\n\nfrom . import db, payments\nfrom .models import CartItem, Order, OrderStatus\n\n\nclass CheckoutError(Exception):\n    pass\n\n\ndef cart_total_cents(items: list[CartItem]) -> int:\n    total = 0\n    for item in items:\n        product = db.get_product(item.product_id)\n        if product is None:\n            raise CheckoutError(f\"unknown product {item.product_id}\")\n        total += product.price_cents * item.quantity\n    return total\n\n\ndef checkout(user_id: int, items: list[CartItem]) -> Order:\n    if not items:\n        raise CheckoutError(\"cart is empty\")\n\n    # Validate availability WITHOUT mutating stock, so a later payment failure\n    # cannot leave anything reserved.\n    for item in items:\n        product = db.get_product(item.product_id)\n        if product is None:\n            raise CheckoutError(f\"unknown product {item.product_id}\")\n        if product.stock < item.quantity:\n            raise CheckoutError(f\"insufficient stock for {product.name}\")\n\n    total = cart_total_cents(items)\n\n    # Charge first. If this raises, no stock was decremented and no order exists.\n    charge = payments.charge_card(total)\n\n    # Payment confirmed: now it is safe to commit the stock changes + order.\n    for item in items:\n        product = db.get_product(item.product_id)\n        product.stock -= item.quantity\n\n    order = Order(\n        id=db.next_order_id(),\n        user_id=user_id,\n        items=list(items),\n        status=OrderStatus.PAID,\n        total_cents=total,\n        payment_id=charge.id,\n    )\n    db.ORDERS[order.id] = order\n    return order\n```\n",
        "expectPass": true
      },
      {
        "id": "try_except_rollback",
        "title": "Try Except Rollback",
        "summary": "Mock shop · checkout (stock rollback on payment failure) — Try Except Rollback approach.",
        "tradeoffs": [
          "Reference checkout candidate (try_except_rollback)."
        ],
        "generatedCode": "### mock_shop/checkout.py\n\n```python\n\"\"\"Checkout flow with stock rollback on payment failure.\n\nReserves (decrements) stock up front, remembering exactly how much was taken from\neach product, then charges inside a try/except. If the charge raises for any\nreason the reservation is rolled back -- every decremented product's stock is\nrestored -- before the error propagates, so a failed payment never leaks a\nreservation and never creates an order. Public API is unchanged so the rest of\nthe mock-shop package keeps working.\n\"\"\"\nfrom __future__ import annotations\n\nfrom . import db, payments\nfrom .models import CartItem, Order, OrderStatus\n\n\nclass CheckoutError(Exception):\n    pass\n\n\ndef cart_total_cents(items: list[CartItem]) -> int:\n    total = 0\n    for item in items:\n        product = db.get_product(item.product_id)\n        if product is None:\n            raise CheckoutError(f\"unknown product {item.product_id}\")\n        total += product.price_cents * item.quantity\n    return total\n\n\ndef checkout(user_id: int, items: list[CartItem]) -> Order:\n    if not items:\n        raise CheckoutError(\"cart is empty\")\n\n    # Reserve stock up front, recording (product_id, quantity) for each\n    # decrement so we can put it back if payment fails later.\n    reserved: list[tuple[int, int]] = []\n    for item in items:\n        product = db.get_product(item.product_id)\n        if product is None:\n            raise CheckoutError(f\"unknown product {item.product_id}\")\n        if product.stock < item.quantity:\n            raise CheckoutError(f\"insufficient stock for {product.name}\")\n        product.stock -= item.quantity\n        reserved.append((item.product_id, item.quantity))\n\n    total = cart_total_cents(items)\n    try:\n        charge = payments.charge_card(total)\n    except Exception:\n        # Payment failed: restore every reserved unit, then re-raise so the\n        # caller still sees the failure (no order is created).\n        for product_id, quantity in reserved:\n            product = db.get_product(product_id)\n            if product is not None:\n                product.stock += quantity\n        raise\n\n    order = Order(\n        id=db.next_order_id(),\n        user_id=user_id,\n        items=list(items),\n        status=OrderStatus.PAID,\n        total_cents=total,\n        payment_id=charge.id,\n    )\n    db.ORDERS[order.id] = order\n    return order\n```\n",
        "expectPass": true
      },
      {
        "id": "no_rollback",
        "title": "No Rollback",
        "summary": "Mock shop · checkout (stock rollback on payment failure) — No Rollback approach.",
        "tradeoffs": [
          "Reference checkout candidate (no_rollback)."
        ],
        "generatedCode": "### mock_shop/checkout.py\n\n```python\n\"\"\"Checkout flow.\n\nDeliberately flawed candidate: it decrements (reserves) stock, then charges, with\nNO rollback if the charge fails. After a declined payment the reserved stock stays\ngone -- a leaked reservation -- even though no order is created. Proves the fixture\ncatches a missing stock-rollback implementation (this candidate should FAIL the\nfeature test while still passing every regression test).\n\"\"\"\nfrom __future__ import annotations\n\nfrom . import db, payments\nfrom .models import CartItem, Order, OrderStatus\n\n\nclass CheckoutError(Exception):\n    pass\n\n\ndef cart_total_cents(items: list[CartItem]) -> int:\n    total = 0\n    for item in items:\n        product = db.get_product(item.product_id)\n        if product is None:\n            raise CheckoutError(f\"unknown product {item.product_id}\")\n        total += product.price_cents * item.quantity\n    return total\n\n\ndef checkout(user_id: int, items: list[CartItem]) -> Order:\n    if not items:\n        raise CheckoutError(\"cart is empty\")\n\n    # Reserve stock (no locking, no rollback if payment fails later).\n    for item in items:\n        product = db.get_product(item.product_id)\n        if product is None:\n            raise CheckoutError(f\"unknown product {item.product_id}\")\n        if product.stock < item.quantity:\n            raise CheckoutError(f\"insufficient stock for {product.name}\")\n        product.stock -= item.quantity\n\n    total = cart_total_cents(items)\n    charge = payments.charge_card(total)\n\n    order = Order(\n        id=db.next_order_id(),\n        user_id=user_id,\n        items=list(items),\n        status=OrderStatus.PAID,\n        total_cents=total,\n        payment_id=charge.id,\n    )\n    db.ORDERS[order.id] = order\n    return order\n```\n",
        "expectPass": false
      }
    ]
  },
  {
    "fixtureId": "mock-shop-payments",
    "module": "payments",
    "targetFile": "mock_shop/payments.py",
    "basename": "payments.py",
    "label": "Mock shop · payments (idempotent charges)",
    "requiredDefs": [
      "\\bdef\\s+charge_card\\s*\\(",
      "\\bdef\\s+refund\\s*\\("
    ],
    "promptHint": "rewrite mock_shop/payments.py so charge_card supports idempotency keys",
    "candidates": [
      {
        "id": "key_in_charge_id",
        "title": "Key In Charge Id",
        "summary": "Mock shop · payments (idempotent charges) — Key In Charge Id approach.",
        "tradeoffs": [
          "Reference payments candidate (key_in_charge_id)."
        ],
        "generatedCode": "### mock_shop/payments.py\n\n```python\n\"\"\"Mock payment gateway with idempotent charges.\n\nAlternative correct design: when an `idempotency_key` is supplied, the charge id is\nderived deterministically from the key (`ch_<key>`) and stored in `_CHARGES`. A repeat\ncall with the same key recomputes the same id, finds the already-stored Charge, and\nreturns it -- so the card is never charged twice. Without a key, charges get a random\nid and are independent, exactly like the baseline. Public API is otherwise unchanged\nso the rest of the mock-shop package keeps working.\n\"\"\"\nfrom __future__ import annotations\n\nimport secrets\nfrom dataclasses import dataclass\n\n\n@dataclass\nclass Charge:\n    id: str\n    amount_cents: int\n    currency: str\n    captured: bool\n    refunded_cents: int = 0\n\n\n_CHARGES: dict[str, Charge] = {}\n\n\nclass PaymentError(Exception):\n    pass\n\n\ndef charge_card(\n    amount_cents: int,\n    currency: str = \"usd\",\n    idempotency_key: str | None = None,\n) -> Charge:\n    if amount_cents <= 0:\n        raise PaymentError(\"amount must be positive\")\n\n    if idempotency_key is not None:\n        # Deterministic id from the key -> the same key maps to the same stored Charge.\n        charge_id = f\"ch_{idempotency_key}\"\n        existing = _CHARGES.get(charge_id)\n        if existing is not None:\n            return existing\n    else:\n        charge_id = f\"ch_{secrets.token_hex(8)}\"\n\n    charge = Charge(\n        id=charge_id,\n        amount_cents=amount_cents,\n        currency=currency,\n        captured=True,\n    )\n    _CHARGES[charge.id] = charge\n    return charge\n\n\ndef refund(charge_id: str, amount_cents: int | None = None) -> Charge:\n    charge = _CHARGES.get(charge_id)\n    if charge is None:\n        raise PaymentError(\"unknown charge\")\n    amount = amount_cents if amount_cents is not None else charge.amount_cents\n    charge.refunded_cents += amount\n    return charge\n```\n",
        "expectPass": true
      },
      {
        "id": "key_registry",
        "title": "Key Registry",
        "summary": "Mock shop · payments (idempotent charges) — Key Registry approach.",
        "tradeoffs": [
          "Reference payments candidate (key_registry)."
        ],
        "generatedCode": "### mock_shop/payments.py\n\n```python\n\"\"\"Mock payment gateway with idempotent charges.\n\nAdds an optional `idempotency_key` to `charge_card`: the first charge for a key is\ncreated and remembered in a key->Charge registry; any later call with the SAME key\nreturns that same Charge instead of charging the card again. Calls with no key (or a\nbrand-new key) are independent, exactly like the baseline. Public API is otherwise\nunchanged so the rest of the mock-shop package keeps working.\n\"\"\"\nfrom __future__ import annotations\n\nimport secrets\nfrom dataclasses import dataclass\n\n\n@dataclass\nclass Charge:\n    id: str\n    amount_cents: int\n    currency: str\n    captured: bool\n    refunded_cents: int = 0\n\n\n_CHARGES: dict[str, Charge] = {}\n# idempotency_key -> the Charge minted for that key (dedupes double-charges)\n_IDEMPOTENT_CHARGES: dict[str, Charge] = {}\n\n\nclass PaymentError(Exception):\n    pass\n\n\ndef charge_card(\n    amount_cents: int,\n    currency: str = \"usd\",\n    idempotency_key: str | None = None,\n) -> Charge:\n    if amount_cents <= 0:\n        raise PaymentError(\"amount must be positive\")\n\n    if idempotency_key is not None and idempotency_key in _IDEMPOTENT_CHARGES:\n        # Same key already charged: return the original Charge, do not double-charge.\n        return _IDEMPOTENT_CHARGES[idempotency_key]\n\n    charge = Charge(\n        id=f\"ch_{secrets.token_hex(8)}\",\n        amount_cents=amount_cents,\n        currency=currency,\n        captured=True,\n    )\n    _CHARGES[charge.id] = charge\n    if idempotency_key is not None:\n        _IDEMPOTENT_CHARGES[idempotency_key] = charge\n    return charge\n\n\ndef refund(charge_id: str, amount_cents: int | None = None) -> Charge:\n    charge = _CHARGES.get(charge_id)\n    if charge is None:\n        raise PaymentError(\"unknown charge\")\n    amount = amount_cents if amount_cents is not None else charge.amount_cents\n    charge.refunded_cents += amount\n    return charge\n```\n",
        "expectPass": true
      },
      {
        "id": "no_idempotency",
        "title": "No Idempotency",
        "summary": "Mock shop · payments (idempotent charges) — No Idempotency approach.",
        "tradeoffs": [
          "Reference payments candidate (no_idempotency)."
        ],
        "generatedCode": "### mock_shop/payments.py\n\n```python\n\"\"\"Mock payment gateway -- deliberately flawed candidate.\n\nIt accepts the `idempotency_key` parameter (so callers passing one don't hit a\nTypeError) but completely ignores it: every call mints a brand-new charge with a\nrandom id. Two calls with the same key therefore produce two different charges -- a\ndouble-charge -- so this candidate FAILS the idempotency feature tests while still\npassing every pre-existing regression (positive-amount check, charge fields, refunds).\n\"\"\"\nfrom __future__ import annotations\n\nimport secrets\nfrom dataclasses import dataclass\n\n\n@dataclass\nclass Charge:\n    id: str\n    amount_cents: int\n    currency: str\n    captured: bool\n    refunded_cents: int = 0\n\n\n_CHARGES: dict[str, Charge] = {}\n\n\nclass PaymentError(Exception):\n    pass\n\n\ndef charge_card(\n    amount_cents: int,\n    currency: str = \"usd\",\n    idempotency_key: str | None = None,  # accepted but never used -> double charges\n) -> Charge:\n    if amount_cents <= 0:\n        raise PaymentError(\"amount must be positive\")\n    # BUG: idempotency_key is ignored, so repeat calls are not deduplicated.\n    charge = Charge(\n        id=f\"ch_{secrets.token_hex(8)}\",\n        amount_cents=amount_cents,\n        currency=currency,\n        captured=True,\n    )\n    _CHARGES[charge.id] = charge\n    return charge\n\n\ndef refund(charge_id: str, amount_cents: int | None = None) -> Charge:\n    charge = _CHARGES.get(charge_id)\n    if charge is None:\n        raise PaymentError(\"unknown charge\")\n    amount = amount_cents if amount_cents is not None else charge.amount_cents\n    charge.refunded_cents += amount\n    return charge\n```\n",
        "expectPass": false
      }
    ]
  },
  {
    "fixtureId": "mock-shop-db",
    "module": "db",
    "targetFile": "mock_shop/db.py",
    "basename": "db.py",
    "label": "Mock shop · db (case-insensitive email lookup)",
    "requiredDefs": [
      "\\bdef\\s+get_user_by_email\\s*\\(",
      "\\bdef\\s+get_product\\s*\\("
    ],
    "promptHint": "rewrite mock_shop/db.py so get_user_by_email is case-insensitive",
    "candidates": [
      {
        "id": "casefold_normalize",
        "title": "Casefold Normalize",
        "summary": "Mock shop · db (case-insensitive email lookup) — Casefold Normalize approach.",
        "tradeoffs": [
          "Reference db candidate (casefold_normalize)."
        ],
        "generatedCode": "### mock_shop/db.py\n\n```python\n\"\"\"In-memory data store for the mock shop. Not production; demo only.\n\nCandidate: case-insensitive email lookup via .casefold() normalization. casefold\nis a more aggressive lowercasing intended for caseless matching; for ASCII email\naddresses it behaves like .lower(), so \"ALICE@EXAMPLE.COM\" matches the seeded\n\"alice@example.com\". Should PASS the feature tests.\n\"\"\"\nfrom __future__ import annotations\n\nfrom .models import Order, Product, User\n\n# Seed users. password_hash is a real sha256 of the password (intentionally weak\n# hashing) so the seeded accounts are loginable: alice -> \"alice-password\",\n# bob -> \"bob-password\". Bench candidates that keep sha256 hashing verify against these.\nUSERS: dict[int, User] = {\n    1: User(\n        id=1,\n        email=\"alice@example.com\",\n        password_hash=\"17a96502d336e4c18a43182a353d7f0a38414c6fc4daf678acae834a819cecee\",\n        is_active=True,\n    ),\n    2: User(\n        id=2,\n        email=\"bob@example.com\",\n        password_hash=\"df53c27a66157885ba143e34f25d6380e12168b0f7da4f0c46efa54cd9a083b7\",\n        is_active=True,\n    ),\n}\n\nPRODUCTS: dict[int, Product] = {\n    10: Product(id=10, name=\"Mechanical Keyboard\", price_cents=12900, stock=5),\n    11: Product(id=11, name=\"USB-C Cable\", price_cents=1500, stock=200),\n    12: Product(id=12, name=\"4K Monitor\", price_cents=39900, stock=2),\n}\n\nORDERS: dict[int, Order] = {}\n_next_order_id = 1000\n\n\ndef next_order_id() -> int:\n    global _next_order_id\n    _next_order_id += 1\n    return _next_order_id\n\n\ndef get_user_by_email(email: str) -> User | None:\n    target = email.casefold()\n    for user in USERS.values():\n        if user.email.casefold() == target:\n            return user\n    return None\n\n\ndef get_product(product_id: int) -> Product | None:\n    return PRODUCTS.get(product_id)\n```\n",
        "expectPass": true
      },
      {
        "id": "lower_normalize",
        "title": "Lower Normalize",
        "summary": "Mock shop · db (case-insensitive email lookup) — Lower Normalize approach.",
        "tradeoffs": [
          "Reference db candidate (lower_normalize)."
        ],
        "generatedCode": "### mock_shop/db.py\n\n```python\n\"\"\"In-memory data store for the mock shop. Not production; demo only.\n\nCandidate: case-insensitive email lookup via .lower() normalization. Both the\nstored email and the query are lowercased before comparison, so \"ALICE@EXAMPLE.COM\"\nmatches the seeded \"alice@example.com\". Should PASS the feature tests.\n\"\"\"\nfrom __future__ import annotations\n\nfrom .models import Order, Product, User\n\n# Seed users. password_hash is a real sha256 of the password (intentionally weak\n# hashing) so the seeded accounts are loginable: alice -> \"alice-password\",\n# bob -> \"bob-password\". Bench candidates that keep sha256 hashing verify against these.\nUSERS: dict[int, User] = {\n    1: User(\n        id=1,\n        email=\"alice@example.com\",\n        password_hash=\"17a96502d336e4c18a43182a353d7f0a38414c6fc4daf678acae834a819cecee\",\n        is_active=True,\n    ),\n    2: User(\n        id=2,\n        email=\"bob@example.com\",\n        password_hash=\"df53c27a66157885ba143e34f25d6380e12168b0f7da4f0c46efa54cd9a083b7\",\n        is_active=True,\n    ),\n}\n\nPRODUCTS: dict[int, Product] = {\n    10: Product(id=10, name=\"Mechanical Keyboard\", price_cents=12900, stock=5),\n    11: Product(id=11, name=\"USB-C Cable\", price_cents=1500, stock=200),\n    12: Product(id=12, name=\"4K Monitor\", price_cents=39900, stock=2),\n}\n\nORDERS: dict[int, Order] = {}\n_next_order_id = 1000\n\n\ndef next_order_id() -> int:\n    global _next_order_id\n    _next_order_id += 1\n    return _next_order_id\n\n\ndef get_user_by_email(email: str) -> User | None:\n    target = email.lower()\n    for user in USERS.values():\n        if user.email.lower() == target:\n            return user\n    return None\n\n\ndef get_product(product_id: int) -> Product | None:\n    return PRODUCTS.get(product_id)\n```\n",
        "expectPass": true
      },
      {
        "id": "exact_only",
        "title": "Exact Only",
        "summary": "Mock shop · db (case-insensitive email lookup) — Exact Only approach.",
        "tradeoffs": [
          "Reference db candidate (exact_only)."
        ],
        "generatedCode": "### mock_shop/db.py\n\n```python\n\"\"\"In-memory data store for the mock shop. Not production; demo only.\n\nDeliberately flawed candidate: keeps the baseline exact-string email match, so an\nuppercase or mixed-case query like \"ALICE@EXAMPLE.COM\" returns None instead of the\nseeded alice user. Proves the fixture catches a missing case-insensitive lookup\n(this candidate should FAIL the feature tests while still passing every regression).\n\"\"\"\nfrom __future__ import annotations\n\nfrom .models import Order, Product, User\n\n# Seed users. password_hash is a real sha256 of the password (intentionally weak\n# hashing) so the seeded accounts are loginable: alice -> \"alice-password\",\n# bob -> \"bob-password\". Bench candidates that keep sha256 hashing verify against these.\nUSERS: dict[int, User] = {\n    1: User(\n        id=1,\n        email=\"alice@example.com\",\n        password_hash=\"17a96502d336e4c18a43182a353d7f0a38414c6fc4daf678acae834a819cecee\",\n        is_active=True,\n    ),\n    2: User(\n        id=2,\n        email=\"bob@example.com\",\n        password_hash=\"df53c27a66157885ba143e34f25d6380e12168b0f7da4f0c46efa54cd9a083b7\",\n        is_active=True,\n    ),\n}\n\nPRODUCTS: dict[int, Product] = {\n    10: Product(id=10, name=\"Mechanical Keyboard\", price_cents=12900, stock=5),\n    11: Product(id=11, name=\"USB-C Cable\", price_cents=1500, stock=200),\n    12: Product(id=12, name=\"4K Monitor\", price_cents=39900, stock=2),\n}\n\nORDERS: dict[int, Order] = {}\n_next_order_id = 1000\n\n\ndef next_order_id() -> int:\n    global _next_order_id\n    _next_order_id += 1\n    return _next_order_id\n\n\ndef get_user_by_email(email: str) -> User | None:\n    for user in USERS.values():\n        if user.email == email:\n            return user\n    return None\n\n\ndef get_product(product_id: int) -> Product | None:\n    return PRODUCTS.get(product_id)\n```\n",
        "expectPass": false
      }
    ]
  },
  {
    "fixtureId": "mock-shop-models",
    "module": "models",
    "targetFile": "mock_shop/models.py",
    "basename": "models.py",
    "label": "Mock shop · models (domain field validation)",
    "requiredDefs": [
      "\\bclass\\s+Product\\b",
      "\\bclass\\s+CartItem\\b"
    ],
    "promptHint": "rewrite mock_shop/models.py to validate domain fields in __post_init__",
    "candidates": [
      {
        "id": "guarded_validate",
        "title": "Guarded Validate",
        "summary": "Mock shop · models (domain field validation) — Guarded Validate approach.",
        "tradeoffs": [
          "Reference models candidate (guarded_validate)."
        ],
        "generatedCode": "### mock_shop/models.py\n\n```python\n\"\"\"Domain models for the mock shop.\n\nSame domain-integrity guarantees as the reference, expressed differently: a\nsingle module-level ``_require(cond, msg)`` guard is reused by each dataclass's\n__post_init__ to reject negative Product price/stock and non-positive CartItem\nquantity. Public shape (OrderStatus enum + User/Product/CartItem/Order\ndataclasses, same field names, types, and defaults) is unchanged.\n\"\"\"\nfrom __future__ import annotations\n\nfrom dataclasses import dataclass, field\nfrom enum import Enum\n\n\ndef _require(cond: bool, msg: str) -> None:\n    \"\"\"Raise ValueError(msg) when a domain invariant is violated.\"\"\"\n    if not cond:\n        raise ValueError(msg)\n\n\nclass OrderStatus(str, Enum):\n    PENDING = \"pending\"\n    PAID = \"paid\"\n    SHIPPED = \"shipped\"\n    REFUNDED = \"refunded\"\n    CANCELLED = \"cancelled\"\n\n\n@dataclass\nclass User:\n    id: int\n    email: str\n    password_hash: str\n    is_active: bool = True\n    failed_logins: int = 0\n\n\n@dataclass\nclass Product:\n    id: int\n    name: str\n    price_cents: int\n    stock: int\n\n    def __post_init__(self) -> None:\n        _require(self.price_cents >= 0, \"price_cents must not be negative\")\n        _require(self.stock >= 0, \"stock must not be negative\")\n\n\n@dataclass\nclass CartItem:\n    product_id: int\n    quantity: int\n\n    def __post_init__(self) -> None:\n        _require(self.quantity > 0, \"quantity must be positive\")\n\n\n@dataclass\nclass Order:\n    id: int\n    user_id: int\n    items: list[CartItem] = field(default_factory=list)\n    status: OrderStatus = OrderStatus.PENDING\n    total_cents: int = 0\n    payment_id: str | None = None\n```\n",
        "expectPass": true
      },
      {
        "id": "post_init_validate",
        "title": "Post Init Validate",
        "summary": "Mock shop · models (domain field validation) — Post Init Validate approach.",
        "tradeoffs": [
          "Reference models candidate (post_init_validate)."
        ],
        "generatedCode": "### mock_shop/models.py\n\n```python\n\"\"\"Domain models for the mock shop.\n\nAdds domain-integrity validation in __post_init__: a Product cannot have a\nnegative price or negative stock, and a CartItem must have a positive quantity.\nThe public shape (OrderStatus enum + User/Product/CartItem/Order dataclasses,\nwith the same field names, types, and defaults) is unchanged so the rest of the\nmock-shop package keeps importing and constructing these types.\n\"\"\"\nfrom __future__ import annotations\n\nfrom dataclasses import dataclass, field\nfrom enum import Enum\n\n\nclass OrderStatus(str, Enum):\n    PENDING = \"pending\"\n    PAID = \"paid\"\n    SHIPPED = \"shipped\"\n    REFUNDED = \"refunded\"\n    CANCELLED = \"cancelled\"\n\n\n@dataclass\nclass User:\n    id: int\n    email: str\n    password_hash: str\n    is_active: bool = True\n    failed_logins: int = 0\n\n\n@dataclass\nclass Product:\n    id: int\n    name: str\n    price_cents: int\n    stock: int\n\n    def __post_init__(self) -> None:\n        if self.price_cents < 0:\n            raise ValueError(\"price_cents must not be negative\")\n        if self.stock < 0:\n            raise ValueError(\"stock must not be negative\")\n\n\n@dataclass\nclass CartItem:\n    product_id: int\n    quantity: int\n\n    def __post_init__(self) -> None:\n        if self.quantity <= 0:\n            raise ValueError(\"quantity must be positive\")\n\n\n@dataclass\nclass Order:\n    id: int\n    user_id: int\n    items: list[CartItem] = field(default_factory=list)\n    status: OrderStatus = OrderStatus.PENDING\n    total_cents: int = 0\n    payment_id: str | None = None\n```\n",
        "expectPass": true
      },
      {
        "id": "no_validation",
        "title": "No Validation",
        "summary": "Mock shop · models (domain field validation) — No Validation approach.",
        "tradeoffs": [
          "Reference models candidate (no_validation)."
        ],
        "generatedCode": "### mock_shop/models.py\n\n```python\n\"\"\"Domain models for the mock shop.\n\nPlain dataclasses with no domain-integrity validation: negative Product price or\nstock and non-positive CartItem quantity are silently accepted. Public shape\n(OrderStatus enum + User/Product/CartItem/Order dataclasses, same field names,\ntypes, and defaults) matches the rest of the package.\n\"\"\"\nfrom __future__ import annotations\n\nfrom dataclasses import dataclass, field\nfrom enum import Enum\n\n\nclass OrderStatus(str, Enum):\n    PENDING = \"pending\"\n    PAID = \"paid\"\n    SHIPPED = \"shipped\"\n    REFUNDED = \"refunded\"\n    CANCELLED = \"cancelled\"\n\n\n@dataclass\nclass User:\n    id: int\n    email: str\n    password_hash: str\n    is_active: bool = True\n    failed_logins: int = 0\n\n\n@dataclass\nclass Product:\n    id: int\n    name: str\n    price_cents: int\n    stock: int\n\n\n@dataclass\nclass CartItem:\n    product_id: int\n    quantity: int\n\n\n@dataclass\nclass Order:\n    id: int\n    user_id: int\n    items: list[CartItem] = field(default_factory=list)\n    status: OrderStatus = OrderStatus.PENDING\n    total_cents: int = 0\n    payment_id: str | None = None\n```\n",
        "expectPass": false
      }
    ]
  }
];

export const MOCK_SHOP_FIXTURE_IDS: string[] = MOCK_SHOP_MODULES.map((m) => m.fixtureId);
